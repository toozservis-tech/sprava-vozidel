[pytest]
testpaths = tests/api
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
markers =
    smoke: Smoke tests
    api: API tests
    auth: Authentication tests
    vehicles: Vehicle tests

